apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shared-resources.fullname" . }}-test-connection"
  labels:
    {{- include "shared-resources.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shared-resources.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

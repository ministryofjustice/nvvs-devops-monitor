apiVersion: v1
kind: Service
metadata:
  name: {{ include "shared-resources.fullname" . }}
  labels:
    {{- include "shared-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shared-resources.selectorLabels" . | nindent 4 }}
